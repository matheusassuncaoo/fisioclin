# ===============================
# PERFIL DE HOMOLOGAÇÃO
# ===============================

# Database - Usar variáveis de ambiente
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Pool de conexões intermediário
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000

# JPA - Logs moderados
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.hibernate.ddl-auto=validate

# Server
server.port=8080

# Logs moderados
logging.level.com.br.fasipe.fisioclin=INFO
logging.level.org.springframework.web=INFO

# Security - Usar variáveis de ambiente
spring.security.user.name=${ADMIN_USERNAME:admin}
spring.security.user.password=${ADMIN_PASSWORD}

# Swagger/OpenAPI
fisioclin.openapi.dev-url=${APP_URL:http://localhost:8080}

# Ativa compressão
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain
